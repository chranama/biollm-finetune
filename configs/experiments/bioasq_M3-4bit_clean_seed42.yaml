experiment:
  name: bioasq_M3-4bit_clean_seed42
  seed: 42
  dataset_split: smoke_integration
  perturbation: clean

  model:
    id: BioMistral/BioMistral-7B
    quantization: "4bit"
    adapter_path: "results/checkpoints/biomistral_bioasq_qlora"  # <-- adjust to your actual adapter dir

  data:
    questions_path: "data/samples/integration_gold.json"

  # Use your existing inference config
  inference_config: "configs/inference.yaml"